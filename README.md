# Profile-
git config --global user. name "Kartik"
git config --global user.email "kartikrao501@gmail.com"
# Source - https://stackoverflow.com/q/21243172
# Posted by void.pointer, modified by community. See post 'Timeline' for change history
# Retrieved 2026-07-16, License - CC BY-SA 4.0

lg1 = log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset)%x09%C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset) %C(dim white)%an%C(reset) - %C(white)%s%C(reset)' --branches --remotes --tags
